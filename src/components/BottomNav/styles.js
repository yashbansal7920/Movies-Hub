import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  root: {
    backgroundColor: "#212121",
    position: "fixed",
    zIndex: 100,
    bottom: 0,
    width: "100%",
  },
  icon: {
    color: "#fafafa",
  },
}));
